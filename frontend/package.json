{
  "name": "bary-template",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "start": "vite build && cargo run --release",
    "build": "vite build && cargo build --release"
  },
  "license": "MIT",
  "devDependencies": {
    "typescript": "^4.9.5",
    "vite": "^4.1.1",
    "vite-plugin-solid": "^2.5.0"
  },
  "dependencies": {
    "solid-js": "^1.6.10"
  }
}
